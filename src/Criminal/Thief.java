package Criminal;
import Police.Cop;

public class Thief {
    public static void main(String[] args) {
        //Cop c = new Cop(100);
        //System.out.println(c);
       // c.canIShoot();
        //For security - Cop() is protected, canISoot() is protected. so the thief cannot access it.
        //A class outside the package cannot access the protected attributes/methods
    }

}
